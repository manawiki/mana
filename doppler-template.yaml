projects:
   - name: "UPDATE"
     environments:
        - name: Production
          slug: prd

     secrets:
        prd:
           METRONOME_API_KEY: ${mana.prd.METRONOME_API_KEY}
           MONGODB_URI: ${mana.prd.MONGODB_URI}
           PAYLOADCMS_SECRET: ${mana.prd.PAYLOADCMS_SECRET}
           PAYLOAD_NODEMAILER_PASSWORD: ${mana.prd.PAYLOAD_NODEMAILER_PASSWORD}
           PAYLOAD_PUBLIC_BACKBLAZE_APPLICATION_KEY: ${mana.prd.PAYLOAD_PUBLIC_BACKBLAZE_APPLICATION_KEY}
           PAYLOAD_PUBLIC_BACKBLAZE_KEYID: ${mana.prd.PAYLOAD_PUBLIC_BACKBLAZE_KEYID}
           STRIPE_PUBLIC_KEY: ${mana.prd.STRIPE_PUBLIC_KEY}
           STRIPE_SECRET_KEY: ${mana.prd.STRIPE_SECRET_KEY}
           FLY_API_TOKEN: ${mana.prd.FLY_API_TOKEN}
           CUSTOM_DB_NAME: "UPDATE"
           FILE_PREFIX: "UPDATE"
